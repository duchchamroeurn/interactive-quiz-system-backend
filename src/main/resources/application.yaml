spring:
  application:
    name: iqs
  datasource:
    url: jdbc:postgresql://localhost:5432/iqsdb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  address: 0.0.0.0
  port: 9099
